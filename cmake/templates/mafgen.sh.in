#!/bin/bash
# mafgen wrapper script - auto-generated by CMake
# Uses the Python venv at: @SDL_VENV_DIR@
#
# Regenerate a MAFGEN DASS-style listing from a composed config build
# (mmu2fcm's <CFG>.fcm + <CFG>.sym.json + per-phase sidecars), or normalize
# a real DASS .ASC for diffing (--strip), or generate-and-score (--score).

exec "@SDL_VENV_PYTHON@" -m mafgen "$@"

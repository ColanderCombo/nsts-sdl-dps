#!/bin/bash
# GPC batch runner wrapper - auto-generated by CMake
# Runs a linked HAL/S program (.fcm) in batch mode via `gpc run`.
#
SIM_DIR="@SIM_DIR@"
GPC_JS="${SIM_DIR}/dist/gpc.js"

exec node "${GPC_JS}" run "$@"
